package com.a525team.weekbrowser;

import android.os.Build;

public class WTF {
    private final static int SDK = Integer.parseInt(String.valueOf(Build.VERSION.SDK_INT));
    public static int SDK_INT(){
        return SDK;
        }
        
}
